## 1. Contrato local y frontera de privacidad

- [x] 1.1 [Backend] Definir esquemas tipados de creación de feedback y resumen agregado que acepten solo campos y vocabularios de `claimvox_feedback_persistence_policy.json`. Evidencia: `FeedbackCreateRequest`, `FeedbackSummaryItem` y `FeedbackSummaryResponse` rechazan propiedades adicionales; `python -m py_compile app/api/schemas/feedback.py` y `git diff --check` superados el 29 de julio de 2026. Las pruebas sintéticas directas corresponden a la tarea 4.2.
- [x] 1.2 [Backend] Extender `docs/api/openapi.json` con operaciones locales versionadas de creación y resumen de feedback, sin incluir narrativa, identidad ni exportación individual. Evidencia: `python -m json.tool docs/api/openapi.json > /dev/null` y `git diff --check` superados el 29 de julio de 2026; las rutas se señalan como `planned-in-change` hasta que exista servicio.

## 2. Servicio local de feedback

- [x] 2.1 [Backend] Implementar el caso de uso local que valide el feedback antes de delegar en el repositorio PG-14 y mantenga la predicción independiente. Evidencia: `FeedbackService` genera identificador y marcas UTC, deriva retención de la política y delega solo `FeedbackRecord`; `python -m py_compile app/api/services/feedback_service.py` y `git diff --check` superados el 29 de julio de 2026. Las pruebas sintéticas directas corresponden a la tarea 4.2.
- [x] 2.2 [Backend] Exponer rutas locales para crear feedback y consultar exclusivamente el resumen agregado, con errores seguros y sin red ni base compartida. Evidencia: `POST /api/v1/feedback` y `GET /api/v1/feedback/summary` se declaran en `app/api/main.py`; `python -m py_compile app/api/main.py` y `git diff --check` superados el 29 de julio de 2026. Las pruebas directas ASGI corresponden a la tarea 4.2.
- [x] 2.3 [Backend] Ejecutar purga idempotente antes del resumen y registrar solo contadores agregados. Evidencia: `FeedbackService.get_summary` exige una marca UTC, purga antes de consultar y valida `FeedbackSummaryResponse`; la ruta delega en el caso de uso. `python -m py_compile app/api/services/feedback_service.py app/api/main.py` y `git diff --check` superados el 29 de julio de 2026. Las pruebas de vencidos, vigentes y salida corresponden a la tarea 4.2.

## 3. Recorrido explícito en ClaimVox

- [x] 3.1 [Frontend] Añadir un cliente de feedback local desacoplado y configurable, sin reutilizar ni persistir la narrativa de predicción. Evidencia: contratos y `createConfiguredFeedbackClient` usan solo metadatos aprobados y exponen `unavailable` sin API local; `npm exec -- tsc --noEmit --pretty false` y `git diff --check` superados el 29 de julio de 2026. Las pruebas de contrato sintético corresponden a la tarea 4.2.
- [x] 3.2 [Frontend] Añadir controles posteriores al resultado para decisiones y finalidades permitidas, con clase revisada solo cuando la decisión lo exija. Evidencia: `PredictionResult.tsx` muestra los controles solo con `local_api`, requiere clase revisada para `corrected` y construye la solicitud desde los metadatos de la respuesta; `npm exec -- tsc --noEmit --pretty false` y `git diff --check` superados el 29 de julio de 2026. Las pruebas de componente corresponden a la tarea 4.2.
- [x] 3.3 [Frontend] Comunicar éxito, indisponibilidad y límites de privacidad de forma accesible, sin bloquear una predicción ni presentar la operación como autenticada o desplegada. Evidencia: `PredictionResult.tsx` comunica éxito, error recuperable, indisponibilidad y límites de datos; `npm exec -- tsc --noEmit --pretty false` y `git diff --check` superados el 29 de julio de 2026. Las pruebas de estado y revisión manual corresponden a la tarea 4.2.

## 4. Recolección gobernada y evidencia

- [x] 4.1 [Datos / arquitectura] Definir la evidencia agregada de candidatos de reentrenamiento y sus límites: no hay corpus, validación, deduplicación ni incorporación automática. Evidencia: `reports/validation/claimvox_feedback_retraining_boundary.md` delimita los metadatos minimizados de `future_retraining_candidate`, la raíz local controlada, la retención máxima de 30 días y la ausencia explícita de corpus, incorporación, Champion y reentrenamiento automático.
- [x] 4.2 [Tests / QA] Ejecutar solo las pruebas nuevas o afectadas de backend y frontend, type-check, `git diff --check` y validación OpenSpec estricta. Evidencia: `python -m unittest tests.unit.test_feedback_operational_flow -v` superado: 3 pruebas, 0 fallos; `npm exec -- vitest run src/components/PredictionResult.test.tsx` superado: 2 pruebas, 0 fallos; `npm exec -- tsc --noEmit --pretty false`, `git diff --check` y `npm exec -- openspec validate add-feedback-operational-flow --type change --strict` superados el 29 de julio de 2026.

## 5. Documentar y cerrar

- [x] 5.1 [Documentación] Actualizar únicamente README, CHANGELOG, niveles de entrega y fuentes NotebookLM cuyo significado cambie; distinguir feedback local implementado de autenticación, operación compartida, reentrenamiento, Docker y despliegue pendientes. Evidencia: `reports/validation/claimvox_feedback_operational_flow.md` recoge flujo, privacidad, retención, límites y comprobaciones directas; OpenAPI, README, CHANGELOG, niveles de entrega y fuentes NotebookLM reflejan creación y resumen locales sin presentar `MED-04` o `MED-05` como verificados.
- [x] 5.2 [Miguel / coordinación] Revisar humanamente la evidencia de `MED-04` y `MED-05`, actualizar Jira `PG-13` con enlaces reales y preparar PR hacia `dev`; no verificar criterios ni archivar sin evidencia completa y revisión humana. Evidencia: revisión humana confirmada el 29 de julio de 2026; comentario de `PG-13` enlaza la PR `#60` fusionada (`https://github.com/Bootcamp-IA-MAD-P7/Proyecto6-Grupo1/pull/60`) y mantiene `MED-04` y `MED-05` en `En curso` por sus límites documentados.
