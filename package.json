{
  "name": "proyecto6-grupo1-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Repository-local tooling for the CFPB multiclass classification project.",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "openspec": "openspec",
    "openspec:doctor": "openspec doctor",
    "openspec:status": "openspec status",
    "openspec:validate": "openspec validate --all --strict --no-interactive"
  },
  "devDependencies": {
    "@fission-ai/openspec": "1.6.0"
  }
}
