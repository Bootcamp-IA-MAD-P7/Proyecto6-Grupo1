# Daily — 2026-07-28

## Estado compartido

- La PR #36 se fusionó en `dev` y Jira `PG-8` quedó en `Listo` con su enlace y evidencias.
- `MED-01` queda verificado: Random Forest, XGBoost y LightGBM se compararon con el baseline sobre una muestra de 50K. XGBoost obtuvo el mayor macro F1 de validation (`0.6332`), pero ningún ensemble queda seleccionado como modelo definitivo porque sus gaps train/validation superan el 5 %.
- La corrección de CI deja LightGBM en CPU por defecto; la GPU permanece disponible solo bajo activación explícita en un entorno compatible. Las pruebas de modelo e integración pasan sin OpenCL.
- Se archiva el cambio OpenSpec `med-01-ensemble-models` tras conservar su especificación vigente, sin alterar las métricas ni ejecutar un nuevo entrenamiento.

## Miguel

- Revisó y resolvió el fallo de portabilidad de LightGBM detectado por GitHub Actions, manteniendo los resultados GPU como evidencia histórica y evitando presentar nuevas métricas CPU.
- Coordinó el cierre de la trazabilidad entre la PR #36, Jira `PG-8`, OpenSpec y la documentación de entrega.

## Víctor

- Entregó la comparativa ensemble de `MED-01` con código reutilizable, informe agregado, figuras de matriz de confusión e importancia de variables, y evidencias versionadas.
- Deja `MED-03` como siguiente responsabilidad de datos: reducir el overfitting, optimizar sin usar el test para seleccionar y justificar cualquier futura selección de modelo.

## Abel

- No hay cambios versionados de frontend en esta fecha. ClaimVox mantiene su alcance de prototipo React PWA con respuestas sintéticas, revisión humana y sin inferencia real.

## José

- No hay cambios versionados de backend en esta fecha. `PG-5` continúa como el siguiente trabajo de servicio de inferencia; su integración real requiere un artefacto de modelo trazable y el contrato ya preparado.

## Decisiones y bloqueos

- `MED-01` acredita una comparación, no un Champion: el mejor resultado de validation no supera el criterio de control de sobreajuste.
- `ESS-07` y `ESS-08` cuentan con evidencia técnica generada por la comparativa; la consolidación del informe y el análisis de errores siguen en el alcance posterior correspondiente.
- No se han creado backend real, inferencia integrada, despliegue ni MLOps.
