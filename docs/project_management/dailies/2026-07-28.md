# Daily — 2026-07-28

## Estado compartido

- La PR #36 se fusionó en `dev` y Jira `PG-8` quedó en `Listo` con su enlace y evidencias.
- `MED-01` queda verificado: Random Forest, XGBoost y LightGBM se compararon con el baseline sobre una muestra de 50K. XGBoost obtuvo el mayor macro F1 de validation (`0.6332`), pero ningún ensemble queda seleccionado como modelo definitivo porque sus gaps train/validation superan el 5 %.
- La corrección de CI deja LightGBM en CPU por defecto; la GPU permanece disponible solo bajo activación explícita en un entorno compatible. Las pruebas de modelo e integración pasan sin OpenCL.
- Se archiva el cambio OpenSpec `med-01-ensemble-models` tras conservar su especificación vigente, sin alterar las métricas ni ejecutar un nuevo entrenamiento.
- `PG-6` conecta ClaimVox y FastAPI de forma local y explícita. El smoke con entrada sintética confirmó servicio activo, preflight CORS, contrato de respuesta, resultado visible como `Prediction response` y `Human review required`, sin conservar el texto enviado.
- La PR #42 fusionó `PG-7`: el baseline se reconstruye con train y validation actuales, sin cargar test, y entrega evidencia agregada de matriz de confusión, importancia y análisis de errores. `ESS-01` y `ESS-07` a `ESS-10` quedan verificados; con ello el nivel esencial alcanza 10 de 10 criterios verificados para ejecución local.
- Se inicia la preparación transversal del corte de MVP: inventario de límites,
  seguridad local y narrativa de presentación. No introduce cuentas reales,
  almacenamiento de reclamaciones, despliegue ni seguimiento de personas.
- Jira incorpora el Epic `PG-9`, el cierre transversal `PG-10` y la secuencia
  posterior `PG-11` a `PG-17`. Son planificación trazable; no cambian ningún
  criterio de entrega ni representan funcionalidades ya construidas.

## Miguel

- Revisó y resolvió el fallo de portabilidad de LightGBM detectado por GitHub Actions, manteniendo los resultados GPU como evidencia histórica y evitando presentar nuevas métricas CPU.
- Coordinó el cierre de la trazabilidad entre la PR #36, Jira `PG-8`, OpenSpec y la documentación de entrega.
- Integró y verificó el recorrido local de `PG-6`: configuración explícita, CORS de mínimo privilegio, respuesta contractual, estados seguros y evidencia agregada. No se configuró despliegue ni se almacenaron narrativas.

## Víctor

- Entregó la comparativa ensemble de `MED-01` con código reutilizable, informe agregado, figuras de matriz de confusión e importancia de variables, y evidencias versionadas.
- Deja `MED-03` como siguiente responsabilidad de datos: reducir el overfitting, optimizar sin usar el test para seleccionar y justificar cualquier futura selección de modelo.

## Abel

- ClaimVox conserva el prototipo PWA, la accesibilidad y el mock seguro por defecto. La integración de `PG-6` reutiliza el contrato y el flujo visual existentes sin alterar su autoría ni presentar la interfaz como producto desplegado.

## José

- La base backend de `PG-5`, ya fusionada, queda verificada contra un artefacto local reproducible: contrato, health, predictor y fallback mock se comportan según lo previsto. `PG-6` añade CORS local explícito para ClaimVox; quedan fuera autenticación, persistencia y despliegue.

## Decisiones y bloqueos

- `MED-01` acredita una comparación, no un Champion: el mejor resultado de validation no supera el criterio de control de sobreajuste.
- `ESS-07` y `ESS-08` cuentan con evidencia técnica generada por la comparativa; la consolidación del informe y el análisis de errores siguen en el alcance posterior correspondiente.
- La integración local de `PG-6` se fusionó mediante la PR #40 y deja `ESS-04`
  verificado. No acredita autenticación, persistencia, despliegue, operación
  pública ni MLOps. La respuesta sigue siendo una recomendación obligatoriamente
  revisable.
- El nivel esencial verificado no acredita despliegue, autenticación, persistencia, operación pública, Champion ni MLOps. El siguiente alcance requiere un cambio OpenSpec específico.
