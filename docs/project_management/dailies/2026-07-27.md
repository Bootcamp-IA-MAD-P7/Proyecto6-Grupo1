# Daily — 2026-07-27

## Estado compartido

- Se ha completado la preparación contractual de datos para `PG-2` / `T-005` y `T-006`.
- El corpus local, las particiones y la etapa intermedia permanecen ignorados por Git; la evidencia versionada contiene solo cifras agregadas.
- No se ha entrenado ningún modelo ni existe inferencia real. `PG-3` queda listo para iniciar un baseline reproducible con validation y test separados.

## Miguel

- Coordinó los cambios OpenSpec de constructor y política, su trazabilidad con `PG-2` y la validación de privacidad de los artefactos.
- Incorporó pruebas sintéticas, manifiestos agregados, split local y documentación de la diferencia entre instantáneas de fuente.

## Víctor

- Su EDA previo continúa siendo la evidencia de distribución, temporalidad, idioma, longitud y duplicados para su instantánea de datos.
- La evidencia EDA ha servido para cerrar la política inicial: inglés, grupos completos, split temporal, soporte mínimo y pesos balanceados. El siguiente hito de datos es el baseline de `PG-3`.

## Abel

- La React PWA integrada sigue disponible como prototipo con mocks y revisión humana; no se modifica en esta intervención de datos.

## José

- Sin cambios de backend registrados hoy. El servicio de inferencia permanece pendiente del baseline evaluado de `PG-3`; las decisiones de preparación de datos ya no son el bloqueo.

## Decisiones y bloqueos

- La fuente oficial descargada el 27 de julio no coincide en tamaño con la instantánea empleada por el EDA anterior; ambas quedan trazadas por huella y no se mezclan sus cifras.
- Se aprueba una política inicial: 1.961.073 filas en inglés, grupos duplicados aislados, split 70/15/15 por tiempo y mínimo de 100 filas por clase en validation y test.
- `T-004`, `T-005` y `T-006` quedan cerradas como trabajo de EDA y preparación. No equivale a haber entrenado, evaluado o desplegado un modelo.
