# Daily — 2026-07-27

## Estado compartido

- `PG-2` deja preparada la fuente local contractual: los datos brutos, corpus y particiones siguen ignorados por Git; los informes versionados contienen únicamente cifras agregadas.
- La evidencia EDA se cierra para `ESS-02`: código reproducible, informe agregado, cuatro figuras y justificación de visualizaciones para texto; las decisiones posteriores se enlazan sin mezclar instantáneas.
- La PR #31 incorpora el baseline reproducible de `PG-3`: TF-IDF + LogisticRegression, macro F1 de validation `0.5973`, gap train/validation `0.0482` y test protegido ejecutado una vez. Esto verifica `ESS-03`, `ESS-05` y `ESS-06`, pero no acredita todavía inferencia integrada ni un producto operativo.
- La PR #28 de frontend está fusionada: ClaimVox mejora identidad visual, preferencias de tema y accesibilidad, manteniendo respuestas sintéticas y revisión humana.

## Miguel

- Coordinó la trazabilidad entre la preparación de datos, OpenSpec, Jira y la evidencia necesaria para continuar desde el baseline sin presentar el prototipo como inferencia real.
- Revisó el impacto documental de los cambios fusionados y mantuvo separados los estados de entrega, los mocks de interfaz y las evidencias de datos.

## Víctor

- Consolidó la evidencia de EDA y preparación que sustenta el baseline: política de idioma, grupos completos, split temporal, soporte mínimo y pesos balanceados.
- Implementó y evaluó el baseline de `PG-3`, con configuración, métricas de validation, test protegido y clases débiles documentadas en `reports/validation/cfpb_baseline.md` y sus JSON asociados.
- Mantiene la responsabilidad de datos para comparar futuros candidatos sin reutilizar el test protegido para seleccionar configuraciones.

## Abel

- La PR #28 de Abel se integró en `dev`: ClaimVox incorpora identidad visual, preferencias claro/oscuro/sistema y ajustes de accesibilidad, manteniendo las respuestas como mock y la revisión humana como límite.

## José

- El backend puede iniciar su definición de servicio y contrato a partir de la evidencia del baseline. La integración real continúa pendiente de un artefacto de modelo trazable y de la implementación del servicio de inferencia.

## Decisiones y bloqueos

- La fuente oficial descargada el 27 de julio no coincide en tamaño con la instantánea empleada por el EDA anterior; ambas quedan trazadas por huella y no se mezclan sus cifras.
- `ESS-02` queda verificado; `ESS-01` sigue en curso porque el artefacto del baseline es local e ignorado por Git, y `ESS-04` permanece pendiente de una integración real de extremo a extremo.
- La política inicial aprobada usa inglés, grupos duplicados aislados, split temporal 70/15/15 y un mínimo de 100 filas por clase en validation y test.
- El criterio de sobreajuste se sustenta en el gap train/validation. El resultado del test protegido es descriptivo de esa evaluación y no demuestra por sí solo generalización a cualquier distribución.
